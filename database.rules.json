{
    "rules": {
        ".read": false,
        ".write": false,
        "publicRooms": {
            ".read": "auth != null",
            "$roomId": {
                ".validate": "newData.hasChildren(['name'])",
                "name": {
                    ".validate": "newData.isString() && newData.val().length > 0"
                },
                "$other": {
                    ".validate": false
                }
            }
        },
        "privateRooms": {
            "$roomId": {
                ".read": "root.child('roomMembers').child($roomId).child(auth.uid).exists()",
                ".write": "newData.exists() && !data.exists()",
                ".validate": "newData.hasChildren(['name, createdBy, createdAt'])",
                "name": {
                    ".validate": "newData.isString() && newData.val().length > 0"
                },
                "createdBy": {
                    ".validate": "newData.isString() && newData.val() == auth.uid"
                },
                "createdAt": {
                    ".validate": "newData.isNumber() && newData.val() > 0"
                },
                "$other": {
                    ".validate": false
                }
            }
        },
        "roomMembers": {
            "$roomId": {
                ".read": "root.child('privateRooms').child($roomId).exists() && root.child('roomMembers').child($roomId).child(auth.uid).exists()",
                ".write": "root.child('privateRooms').child($roomId).child('createdBy').val() == auth.uid || root.child('roomMembers').child($roomId).child(auth.uid).val() == true",
                "userId": {
                    ".validate": "newData.isBoolean()"
                },
                "$other": {
                    ".validate": false
                }
            }
        },
        "messages": {
            "$roomId": {
                ".read": "root.child('publicRooms').child($roomId).exists() || root.child('roomMembers').child($roomId).child(auth.uid).val() == true",
                ".write": "newData.exists() && !data.exists() && (root.child('publicRooms').child($roomId).exists() || root.child('roomMembers').child($roomId).child(auth.uid).val() == true)",
                ".indexOn": [
                    "createdAt"
                ],
                "$message": {
                    ".write": "newData.exists() && !data.exists() && newData.child('createdBy').val() == auth.uid",
                    "content": {
                        ".validate": "newData.isString() && newData.val().length > 0"
                    },
                    "createdAt": {
                        ".validate": "newData.isNumber() && newData.val() > 0"
                    },
                    "createdBy": {
                        ".validate": "newData.isString() && newData.val().length > 0"
                    },
                    "$other": {
                        ".validate": false
                    }
                }
            }
        },
        "messageSeenBy": {
            "$roomId": {
                ".read": "root.child('privateRooms').child($roomId).exists() && root.child('roomMembers').child($roomId).child(auth.uid).exists()",
                ".write": "root.child('privateRooms').child($roomId).exists() && root.child('roomMembers').child($roomId).child(auth.uid).exists()",
                "$message": {
                    ".write": "newData.child('userId').val() == auth.uid",
                    ".validate": "newData.hasChildren(['userId', 'timestamp'])",
                    "userId": {
                        ".validate": "newData.isString() && newData.val().length > 0"
                    },
                    "timestamp": {
                        ".validate": "newData.isNumber() && newData.val() > 0"
                    }
                }
            }
        }
    }
}